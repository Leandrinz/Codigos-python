----- Listas (Parte 1) -----
Diferente das Tuplas, as Listas são MUTÁVEIS
Para usar Listas, vc usa colchetes : []

- Para Adicionar elementos a lista no meio do programa se utiliza:
    lanche.append('cookie')
    O 'cookie' vai ficar no final da lista

- Adicionando elemento entre outros elementos: 
    lanche.insert(posição que vc quer, 'elemento')

- Como apagar elementos:
    del lanche[3]
    lanche.pop(3)
    lanche.remove('nome do elemento')
    Ambos servem para apagar um elemento x, vai de vc qual escolher
    obs: lanche.pop() vai eliminar o ultimo elemento
    obs2: tem como verificar se determinado elemento está na lista:   
            if 'Elemento' in lanche:
                lanche.remove('Elemento')

- Formas de Declarar uma lista:
    valores = list(range(4,11))
    valores = [8,2,5,4,9,3,0]
    valores.sort() # Vai colocar em ordem crescente
    valores.sort(reverse=True) # Vai colocar na ordem reversa

- Saber o tamanho da Lista:
    len(valores)

- Ligação entre listas
    Se eu crio:
        a = [2,3,4,5]
        b = a
        b[2] = 8
        Isso vai alterar as DUAS listas
- Pra criar uma lista sem ligação:
b = a[:]